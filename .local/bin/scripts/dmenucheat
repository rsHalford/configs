#!/bin/sh

query=$(dmenu | tr ' ' '+')

if [ "$query" ]; then
	query=$(curl -s "cht.sh/""$query""?TQ")
  echo "$query" | xclip -sel primary
  dunstify "cht.sh" "$query"
else
	dunstify "cht.sh" "No query, closing."
fi
